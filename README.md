# COO Dashboard — Headcorn/Megamind

Центр управления для операционного директора.

## 🚀 Быстрый старт

### Локальный запуск:

```bash
# Перейти в папку проекта
cd coo-dashboard

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

Открыть http://localhost:3000

---

## 📦 Деплой на Railway

### Способ 1: Через GitHub (рекомендуется)

1. **Создать репозиторий на GitHub:**
   - Зайти на https://github.com/new
   - Создать новый репозиторий (например, `coo-dashboard`)

2. **Загрузить код:**
   ```bash
   cd coo-dashboard
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/coo-dashboard.git
   git push -u origin main
   ```

3. **Подключить к Railway:**
   - Зайти на https://railway.app
   - Нажать "New Project"
   - Выбрать "Deploy from GitHub repo"
   - Выбрать репозиторий `coo-dashboard`
   - Railway автоматически определит Next.js и задеплоит

4. **Получить URL:**
   - После деплоя Railway выдаст URL вида `https://coo-dashboard-xxx.up.railway.app`

### Способ 2: Через Railway CLI

```bash
# Установить Railway CLI
npm install -g @railway/cli

# Войти в аккаунт
railway login

# Создать проект
railway init

# Задеплоить
railway up
```

---

## 📱 Структура проекта

```
coo-dashboard/
├── app/
│   ├── page.tsx              # Главный дашборд
│   ├── departments/page.tsx  # Трекер по отделам
│   ├── one-on-one/page.tsx   # 1:1 встречи
│   ├── initiatives/page.tsx  # Стратегические инициативы
│   ├── weekly/page.tsx       # Еженедельный обзор
│   └── problems/page.tsx     # Проблемы и решения
├── components/
│   ├── Sidebar.tsx           # Боковое меню
│   ├── Card.tsx              # Карточка
│   ├── StatusBadge.tsx       # Бейдж статуса
│   ├── MetricCard.tsx        # Карточка метрики
│   └── EditableText.tsx      # Редактируемый текст
└── ...
```

---

## ⚙️ Будущие улучшения

- [ ] Подключить базу данных (PostgreSQL)
- [ ] Добавить авторизацию
- [ ] Интеграция с ПланФиксом
- [ ] Уведомления в Telegram
- [ ] Мобильная версия

---

## 🔒 Безопасность

Сейчас данные хранятся в памяти браузера. При перезагрузке страницы данные сбрасываются.

Для постоянного хранения нужно подключить базу данных.

---

**Версия:** 1.0.0  
**Для:** Headcorn/Megamind

